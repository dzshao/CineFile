#ifndef SCRAPE_WEBSITE
#define SCRAPE_WEBSITE

#include <string>
#include <iostream>

using std::cout;
using std::string;

class ScrapeWebsite {
    public:
        static string & scrapeSite(const string &) 
};

#endif